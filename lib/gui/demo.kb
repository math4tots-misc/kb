import gui

gui.Init()

Main()

fn Main
    while true
        events = gui.Poll()
        if events
            if ['Key', 'Escape'] in events
                return
            end
            print events
        end
        SLEEP(1 / 30)
    end
end

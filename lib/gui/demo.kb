import gui

gui.Init()

Main()

fn Main
    start = TIME()
    while TIME() < start + 10
        events = gui.Poll()
        if events
            if ['Key', 'Escape'] in events
                return
            end
            print events
        end
        SLEEP(1 / 30)
    end
end
